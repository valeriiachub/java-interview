**In which cases it's better to use for loop rather than iterator?**  
If you need to jump through some iteration steps and you have indexes(like iterate every odd element) it's much simpler to use for loop.
